syntax = "proto3";

package GameAPI;

option go_package = "./rpc/;rpc";

service GameAPI {
	rpc SendCmd (CmdRequest) returns (CmdResponse);
}

message CmdRequest {
	string server = 1;
	string command = 2;
}

message CmdResponse {
	string response = 1;
}
